{
  "name": "corne-layouts",
  "private": true,
  "description": "Vial layouts for a Corne keyboard. Edit the readable JSON in src/, build produces the .vil files Vial imports.",
  "scripts": {
    "build": "node scripts/build.js",
    "format": "node scripts/format.js"
  }
}
